# OS
.DS_Store
Thumbs.db

# Editors
.vs/
.idea/
*.swp

# Stray C# project files (if opened outside a Unity project)
*.csproj
*.sln
obj/
bin/

# Built package zips -- release.yml builds these in CI and attaches them
# to the GitHub Release; they don't belong in the repo.
*.zip

# Internal-only docs -- kept locally, not published (README.md is the
# only markdown file meant to be public)
DesignDoc_avatar-vcs.md
DesignDoc_v1_superseded.md
PRD_avatar-vcs.md
USAGE.md
CODE_REVIEW.md
TODO.md
LESSONS.md
MANUAL_TEST.md
.claude/skills/
plan.md
# CI writes this to fingerprint the package file set for the Library cache
.package-file-list
.agents/

# Commit history the TestProject writes to ProjectSettings while running tests
TestProject/ProjectSettings/AvatarVcs/


