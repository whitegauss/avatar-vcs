# avatar-vcs 使い方ガイド

このツールの基本的な考え方と、操作方法をまとめたガイドです。導入手順は [README.md](README.md) を参照してください。

## 基本的な考え方

- **コンテナ**: `[AvatarVCS]` ルートの下に置く、衣装や髪型などの「ひとまとまりの構成」。中に Prefab インスタンスを配置します。
- **コミット**: ある時点でのコンテナ構成(と一部のアバター設定)を記録したスナップショット。
- **ブランチ**: コミットの流れに付けた名前。「main」「hair-short」のように、並行する構成を分けて管理できます。

**重要な前提**: このツールは Prefab や Material の**中身そのものは保存しません**。「どのアセットを使っていたか」を記録・復元するだけです。アセット自体を書き換えて壊してしまった場合、このツールでは元に戻せません(アセットのバックアップは別途行ってください)。

## 元の状態に戻す方法

**コミットを削除しても元に戻ることはありません。** 削除はただの掃除機能です。「戻す」には **Checkout**(チェックアウト)を使います。

1. `Window > AvatarVCS` (またはアバターを選択して `GameObject > AvatarVCS > Open Window`) でウィンドウを開く
2. 左側の History パネルから、戻りたい時点のコミットをクリックして選択
3. 下の「**Checkout Selected Commit**」ボタンを押す

これでシーンがそのコミット時点の構成に戻ります。

### コミットしていない変更は消える(Ctrl+Z で復帰)

Checkout やブランチ切り替えは、**コミットしていないシーン上の変更を保存せずに上書きします**。誤って大事な変更を消さないよう、コミットしていない変更がある状態で Checkout /ブランチ切り替えをしようとすると確認ダイアログが出ます。

もし誤って実行してしまった場合は、Unity 標準の **Ctrl+Z(Undo)** で元に戻せます。逆に、一度でも Commit してある状態であれば、いつでも History からその時点へ Checkout し直せます。

## コミットが削除できないとき

History パネルの「x」ボタンが押せない(グレーアウトしている)場合、そのコミットは**現在のブランチの先頭(`*` が付いているもの)** です。

先頭のコミットを削除できないのは、削除するとブランチがどのコミットも指さなくなってしまうためです。削除したい場合は:

1. まず別のコミットへ「Checkout Selected Commit」する(先頭が別のコミットに移る)
2. 先頭でなくなった元のコミットを選んで「x」で削除する

「x」が押せるのにエラーダイアログが出る場合は、**別のブランチの先頭**になっているコミットです。そのブランチに切り替えてから同様に別コミットへ Checkout し、戻ってきてから削除してください。

## ブランチ比較モード(Compare)

「髪ロング版とショート版を交互に見比べたい」といったときに使います。

1. History に2つ以上コミットがある状態で、Compare の欄で比較したい2つのコミットを選ぶ
2. 「Start Compare」で比較モードに入る(このとき現在の状態は必要な場合のみ安全のため自動コミットされます)
3. 「Show A / Show B」ボタンで**コミットを作らずに**交互に切り替えて見比べられます
4. 終わったら:
   - 「**Keep This as Current State**」: 今表示中の側をそのまま採用してコミットする
   - 「**Restore Original**」: 比較モードに入る前の状態に戻す

比較モード中は他のパネルが隠れます。ウィンドウを閉じたり別のアバターに切り替えたりすると、自動的に「Restore Original」相当の処理が走り、比較モードのまま放置されることはありません。

## アセットの再インポートで壊れたとき(GUID 再マッピング)

Prefab や Material をプロジェクト外からもう一度インポートし直すなどして GUID が変わってしまうと、古いコミットから Checkout しようとしたときに「missing prefabs」として止まります。

その場合、ウィンドウ上部に再マッピング用の欄が表示されるので、表示されている GUID に対して新しい方のアセットを割り当てて「Apply Remapping and Retry」を押してください。一度マッピングすると、以降は自動的に新しい方が使われます。

## まとめ: よくある操作

| やりたいこと | 操作 |
|---|---|
| 今の状態を記録したい | History 下の Commit バーでメッセージを入れて Commit |
| 前の状態に戻したい | History でコミットを選んで Checkout Selected Commit |
| 戻したけどやっぱり戻す前がよかった | コミットしていた時点なら History から選んで Checkout / まだコミットしていなければ Ctrl+Z |
| 髪ロングとショートを見比べたい | Compare で2コミットを選び Start Compare → Show A/B |
| 要らない履歴を消したい | 先頭でないコミットの「x」を押す |
