# PRD: avatar-vcs — VRChat アバター構成バージョン管理システム

> このドキュメントは PRD テンプレートを兼ねています。各セクションの `【テンプレ説明】` は書き方の指針、その下の本文は今回の企画に沿ったドラフトです。実際に運用する際は `【テンプレ説明】` 行を削除してください。

---

## 0. ドキュメント情報

| 項目 | 内容 |
|---|---|
| プロダクト名 | avatar-vcs |
| リポジトリ | `github.com/<your-username>/avatar-vcs` |
| VPM パッケージ ID | `com.<your-username>.avatar-vcs` |
| 英語タグライン | Version control for your VRChat avatar configurations |
| ドキュメントステータス | Draft |
| 作成日 | 2026-08-16 |
| 想定リリース形態 | VPM 配布 + BOOTH（決済/ライセンス窓口） |
| 対象プラットフォーム | Unity 2022.3.x / VRChat SDK3 (Avatars) / NDMF 環境 |
| プロダクト種別 | Unity Editor 拡張（NDMF プラグイン + EditorWindow群、VPM配布） |

---

## 1. 概要 / エレベーターピッチ

**【テンプレ説明】** このプロダクトが「誰の」「どんな課題を」「どう解決するか」を3〜4文で。技術詳細ではなく価値で語る。

改変を繰り返す VRChat ユーザーは、同じ着せ替え・スケール・軽量化設定を毎回手作業でやり直している。本ツールは、それらの設定を「あるべき状態の宣言（JSON）」として扱い、Terraform のように plan（差分確認）→ apply（適用）で再現・横展開できる NDMF プラグインである。Modular Avatar / Avatar Optimizer の上に薄い「設定のコード化レイヤー」を重ね、複数アバターへの安全なインスタンス化を実現する。

### 1.1 設計思想: Terraform アナロジー

本プロダクトの核となる設計思想は「Unityのアバター改変に、Terraform的な宣言的インフラ管理のモデルを輸入する」こと。この対応関係が実装判断の指針になる。

| Terraform | 本ツール |
|---|---|
| `.tf`（宣言的な設定コード） | JSON スナップショット（あるべき設定状態の宣言） |
| `terraform plan` | apply 前の差分プレビュー（追加/変更/解決不能な参照） |
| `terraform apply` | 設定の適用（Hierarchy への反映） |
| `terraform.tfstate` | 現在のアバターの実際の状態（Hierarchy 自体が state） |
| Provider / Resource | MA / AAO の各コンポーネント |
| `terraform destroy` / ロールバック | ローカル履歴からのフォールバック |
| Terraform Registry / module 共有 | プリセット（テンプレ）の共有・販売（v1.x、セクション5.3参照） |
| `sensitive = true` 変数指定 | エクスポート時のみマスクされる Secret フィールド（セクション6.5参照） |
| `-target`（特定リソースのみ操作） | 選択的エクスポート（セクション6.6参照） |

JSON は単なる「設定のセーブ/ロードデータ」ではなく、**「あるべき状態の宣言」** として扱う。現在の Hierarchy という実際の state との差分を計算し、確認（plan）してから反映（apply）する、という一貫したモデルを崩さないことが、機能追加時の判断基準になる。

**本質的な価値は「再現可能なインスタンス化」。** バージョン履歴やフォールバックは付随的な安心材料であり、コアの価値は「同じ JSON 定義から、何度でも・別アバターでも同じ状態を組み立てられる」こと（Terraform が同じ `.tf` から同じインフラを再現するのと同じ構造）。「JSON（設定）→ apply → 完成したアバター（インスタンス）」という一方向パイプラインが本プロダクトの軸である。

---

## 2. 背景と課題

**【テンプレ説明】** なぜ今これを作るのか。ユーザーの痛み、既存手段の限界、タイミングを、できれば一次情報や観察に基づいて書く。

### 2.1 改変作業に残る「めんどくささ」

VRChat の改変作業は非破壊ツール（Modular Avatar 等）の普及で以前より容易になったが、依然として以下が残る:

- 着せ替えは対応衣装なら Setup Outfit 一発だが、非対応衣装ではボーン設定・スケール・貫通対策の手作業が必要になる。
- スケールやフィット調整の「うまくいった値」を保存して次に使い回す標準的な仕組みがない。
- 軽量化（Avatar Optimizer）の設定は単発では強力だが、複数アバター/複数衣装へ横展開する仕組みがない。
- **衣装・アバターがアップデートされると、色調整やフィット調整をやり直すことになる。**

パッケージ管理層（VPM）は VCC / ALCOM が完全にカバー済みで、環境構築の敷居は下がった。だからこそ「導入後の改変作業そのもの」を効率化するレイヤーに需要が移っている、というのが本企画のタイミング上の根拠。

### 2.2 前提の修正: Git は既に一定層に浸透している

**重要な認識:** コミュニティ調査によれば、Git + 適切な `.gitignore` によるプロジェクト管理は技術寄りの改変勢に**かなり普及している**。「壊してもすぐ戻せる安心感」が評価され、非破壊改変ツール（MA / VRCFury / NDMF）との組み合わせで実用的に回っているケースが多い。

したがって本プロダクトは「**Git が使えない人向けの簡易版**」として位置づけると弱い。ターゲットである中級者以上は、むしろ Git を使えるか、使っている層と重なる。差別化は「Git より簡単」ではなく「**Git ではできないこと**」に置く必要がある。

### 2.3 差別化: Git ではできないこと

| 観点 | Git | 本ツール |
|---|---|---|
| 管理単位 | プロジェクト全体（1リポジトリ = 1プロジェクト） | **アバター単位・コンテナ単位** |
| 部分的な切り出し | 不可（コミット全体が単位） | **コンテナ単位で抽出・他アバターへ横展開** |
| ブランチ切り替え速度 | 大量のバイナリアセットをチェックアウトするため遅い | **設定 JSON のみを読んで再生成するため高速** |
| 比較体験 | ファイル差分（人間には読めない `.prefab` YAML） | **「髪A vs 髪B」をコンテナ単位で見比べ・即切り替え** |
| 対象範囲 | Unity プロジェクトのファイル全体 | **アバター構成という意味単位** |
| 配布 | リポジトリ丸ごと（アセット含むため権利上不可） | **設定 JSON のみ（アセット非包含で配布可能）** |

**要点:** Git はファイルを管理する。本ツールは**アバターの構成という意味単位**を管理する。Git で `.prefab` の YAML 差分を見ても何が変わったか分からないが、本ツールなら「髪が Long から Short に変わった」と読める。この抽象度の差が価値の源泉である。

### 2.4 技術的根拠: なぜ Git の重厚さが不要か

改変作業で管理したいのは MA/AAO コンポーネントのスクリプト自体（ロジック）ではなく、その**パラメータ値**と**構成の宣言**にすぎない。スクリプトはほぼ変更されず、変わるのは軽量な設定値だけ。この情報量の違いにより、参照パス＋値さえ保持していれば C# 側で読み書き・復元が完結する。

> **一言でまとめると:** Git 的な「履歴の安心感」は提供するが、Git そのもの（リポジトリ運用・コンフリクト解決・GitHub 連携）は使わない。両者は排他ではなく、**Git でプロジェクト全体を管理しつつ、本ツールでアバター構成を管理する併用も自然**である。

---

## 3. ターゲットユーザー / ペルソナ

**【テンプレ説明】** 主要ペルソナ1〜2名を、行動と動機ベースで。「初心者/上級者」だけでなく具体的な状況で。

- **主要ペルソナ A: 多頭飼い改変勢**
  販売/無料アバターを数十体所有し、全部アップロードして使い分ける。同じ衣装や軽量化設定を複数アバターに適用したい。手作業の反復に強い不満。
- **主要ペルソナ B: 改変代行・ショップ運営者**
  複数案件を並行。案件ごとの構成を再現・切り戻しできる状態で管理したい。納品物のバージョン管理ニーズが強い。
- **主要ペルソナ C: Git 併用の技術寄り個人勢**
  既に Git でプロジェクトを管理している。ただし「`.prefab` の YAML 差分が読めない」「ブランチ切り替えが重い」「衣装単位で他アバターに移せない」ことに不満。**Git を置き換えたいのではなく、Git では届かない粒度を補いたい**（セクション2.3）。

**ターゲット外（明示的に外す）:** 完全初心者。ワンクリックを求める層は既存の MA / Setup Outfit で十分であり、本ツールはあえて「中級者以上」に絞る。

---

## 4. ゴールと成功指標 (KPI)

**【テンプレ説明】** 定性ゴールと、それを測る定量指標を分けて書く。計測できない指標は避ける。

- **プロダクトゴール:** 「同じ改変設定を、別アバターや別セッションで数分で再現できる」状態を作る。
- **成功指標（例、リリース後に調整）:**
  - BOOTH での DL 数 / 30日リテンション（「お布施」型のため収益額そのものはKPIにしない、セクション10参照）
  - 1ユーザーあたりの保存プリセット数（使い回しが起きている証拠）
  - **apply 実行回数**、**別アバターへの横展開回数**（コア価値である「再現可能なインスタンス化」が実際に起きているかの直接指標）
  - 「設定復元（フォールバック）」機能の利用率
  - サポート問い合わせのうち「権利/バックアップ関連の事故」が占める割合（低いほど良い）

---

## 5. スコープ

### 5.1 In Scope（MVP）

**【テンプレ説明】** MVP で必ず作るものだけを列挙。迷ったら次のバージョンへ。

1. 現在のアバターの改変設定（MA / AAO コンポーネントの配置とパラメータ）を JSON スナップショットとして保存。
2. 保存した JSON を別アバター/別セッションに適用（apply）。適用前に差分プレビュー（追加/変更/解決不能な参照）を表示。
3. ツール内蔵のローカル履歴（チェックポイント）機能。ラベル付け・一覧・「この時点に戻す」。
4. アセット本体を一切含まない安全なエクスポート（GUID + パラメータのみ）。
5. `.gitignore` テンプレート同梱（購入アセットのコミット事故防止）。

### 5.2 Out of Scope（将来 / 非対応）

**【テンプレ説明】** 「やらないこと」を明示するのが揉めないコツ。

- クラウド CD / 無人自動アップロード（VRChat の認証仕様・ToS 上のリスクが高いため非対応。半自動の連続ビルドキューは将来検討）。
- Unity 純正 prefab YAML の直接書き換え（fileID/GUID 依存で破綻しやすいため採用しない）。
- **Prefab（衣装等）のインスタンス化・配置、GameObject の新規生成・親子関係の変更**（v1.x 検討）。本ツールの守備範囲は「配置済みの構成に対する設定の再現」であり、「構成そのものの自動生成」ではない。衣装の配置は従来どおりユーザーが手動で行い、その後の MA/AAO パラメータ設定を本ツールが管理する（設計書 4.5 参照）。
- 完全自動のボーンフィッティング精度保証（自動＋手動微調整の併用とする）。

> プリセット（テンプレ）の共有・販売は MVP の Out of Scope だが、v1.x での提供を見込む機能として 5.3 に切り出す（詳細はセクション9.3、13参照）。

### 5.3 将来スコープ: プリセット共有（v1.x）

BOOTH では「特定アバター×特定衣装向けの設定データ」を売るテンプレ販売文化が既に定着しており、本ツールの JSON 形式（アセット本体を含まず GUID 参照のみ）はこの用途と相性が良い。Terraform Registry（モジュール共有）に相当する機能として位置づける。

- **前提の明示:** 「対応アセットを持っている人だけが使える」ことを配布時に明示。GUID が一致しない環境では 6.2 の差分プレビュー（解決不能な参照の警告）がそのまま安全装置として機能する。
- **含めないもの:** アセット本体（メッシュ/テクスチャ）は一切含まない。パラメータと GUID 参照のみ。
- **販売経路:** MVP のツール本体と同じく BOOTH を窓口とする想定。ただしこれは「ツール開発者の収益」ではなく「**プリセット作者（＝ツールのユーザー）が自分の設定を売れるようにする**」機能である点に注意（セクション10の「お布施」方針とは別軸）。
- **法務面:** ユーザーが自作した設定を売る形なので、権利関係の一次責任はプリセット作者側にあるが、ツール側でも「アセット本体を含まない」ことを技術的に保証し、配布時の注意書きテンプレートを用意することでトラブルを予防する。

---

## 6. 主要機能要件

**【テンプレ説明】** 機能ごとに「ユーザーストーリー」と「受け入れ条件」をセットで。

### 6.1 設定スナップショット
- ストーリー: 改変勢として、現在の設定をワンボタンで保存したい。次回また使うため。
- 受け入れ条件:
  - MA / AAO の主要コンポーネントのフィールドを取得し JSON 化できる。
  - 参照は「Transform 相対パス」＋「アセット GUID」で表現し、セッションをまたいで解決可能。
  - 出力は KB オーダーで、バイナリを含まない。
  - JSON ルートに `schemaVersion` を含める（セクション14の規格化に備えた必須項目）。

### 6.2 設定適用（apply）と差分プレビュー
- ストーリー: 別アバターに同じ設定を当てたい。ただし事故は避けたい。
- 受け入れ条件:
  - 適用前のプレビューは以下を **構造化して** 表示する（Terraform plan らしさの生命線のため、単純な件数表示に留めない）:
    - Hierarchy 相対パス
    - コンポーネント種別
    - フィールド名
    - 変更前 → 変更後の値
    - 解決不能な場合はその理由（パス不一致 / GUID 不一致 など）
  - 解決不能な参照があっても、可能な範囲のみ適用 or キャンセルを選べる。
  - **設定ドリフト（乖離）検知:** ツールを経由せず手動で MA/AAO の値が変更されていた場合、apply 実行時にその手動変更分が JSON の値で上書き・破棄されることを、プレビュー上で明確に警告する（「現在の値」と「JSON側の値」が異なる行を強調表示するなど）。

### 6.3 ローカル履歴 / フォールバック
- ストーリー: いろいろ試した後、壊れたら前の状態に戻したい。
- 位置づけ: **コアは plan/apply による再現性。履歴機能は「安心材料」としてMVPに含めるが、優先度は plan/apply より低い。** ブランチ分岐やマージなど高度な履歴操作は将来検討（v1.x以降、本セクションのスコープ外）。
- 受け入れ条件:
  - チェックポイントを時系列で一覧表示、ラベル付け可能。
  - 任意のチェックポイントへ復元できる。復元は設定レイヤーのみを対象とし、重いバイナリのバックアップは行わない。
  - **復元は Replace モード（あるべき状態への収束）で行う。** JSON にないコンポーネントは削除される。ただし削除対象は「本ツールが管理範囲として記録しているもの」に限定し、ツール外で追加されたものは触らない（設計書 4.4 参照）。
  - **削除が発生する場合は差分プレビューで赤字強調＋件数サマリを表示し、明示的な確認チェックを経ないと実行できない。**
  - **復元実行の直前に自動でチェックポイントを作成する**（「復元したがやはり戻す前が良かった」に対応するため）。
  - 復元前に差分プレビュー（6.2 と同じ仕組み）を通す。
  - **Unity 標準 Undo との関係を明記:** 本ツールの apply/復元操作は独自の履歴機能で管理し、Unity 標準の `Ctrl+Z`（Undo システム）との統合は MVP では対応しない（`Undo.RecordObject` 連携は将来検討、セクション13参照）。ユーザーには「元に戻すのはツール内の履歴パネルから」と明示する。

### 6.4 安全なエクスポート/インポート
- 受け入れ条件:
  - エクスポートファイルにアセット実体が含まれないことをコードレベルで保証。
  - インポート時に「配布元の規約を確認してください」の注意を表示。
  - **内部保持用 JSON とエクスポート用 JSON のシリアライズ処理を明確に分離する。** 内部用はローカルでの確認・編集のためすべての値を保持し、エクスポート用は 6.5 の sensitive フィールドを反映した別処理系とする（同じシリアライザを使い回して条件分岐する実装は事故りやすいため避ける）。

### 6.5 Secret / センシティブフィールドのマスキング
- ストーリー: テンプレとして共有・販売する際、体格の生値や内部的な調整ノウハウなど、公開したくないフィールドは隠したい。ただし自分のローカル環境では普通に確認・編集したい。
- 発想元: Terraform の `sensitive = true` 変数指定と同じモデル。ただし Terraform では `.tfstate` に平文で残る点は本ツールでは踏襲しない（6.4 の分離処理で対応）。
- MVP スコープ: **ユーザーが明示的に `sensitive` フラグを付けたフィールドのみ**を対象とする。自動検出（体格値らしきフィールドを推測してフラグを提案する等）は行わない。実装コストと誤検知リスクを抑えるため、v1.x 以降の拡張候補とする。
- 受け入れ条件:
  - 任意のフィールドに `sensitive` フラグを付けられる（スキーマレベルで定義）。
  - ローカルでの確認・編集・apply には一切影響しない（値は正常に使われる）。
  - **エクスポート（共有/販売用）時のみ**、sensitive フィールドを完全に除外またはマスク（ダミー値で上書き）し、エクスポート前に「◯件のセンシティブな値が含まれています」と警告する。
  - 誤って sensitive フィールドを含んだままエクスポートしてしまう事故を防ぐのが主目的（デフォルトは除外、明示的操作でのみ含める）。

### 6.6 選択的エクスポート（Partial Export）
- ストーリー: 「このprefabのこのシェイプキー調整だけ」のように、スナップショットの一部だけを切り出してテンプレ販売したい。
- 発想元: Terraform の `-target`（特定リソースのみ plan/apply）、あるいは大きな state から一部だけを切り出すイメージ。
- **段階的実装方針:**
  - **MVP: コンポーネント単位の選択エクスポート。** `components` 配列から対象パス/コンポーネントをチェックボックスで選び、選択分だけを含む JSON を出力する。実装コストが低く（配列フィルタのみ）、既存のデータモデルをそのまま使える。
  - **v1.1: フィールド/配列要素単位の選択エクスポート。** BlendShape（シェイプキー）のような配列フィールドは、要素ごと（シェイプキー名単位）に選択できるようにする。BlendShape配列を名前ベースでインデックス化する追加実装が必要（セクション8/設計書参照）。
- 受け入れ条件（MVP範囲）:
  - UI 上でコンポーネント/パスのチェックボックスツリーから対象を選べる。
  - 選択されなかったコンポーネントは、エクスポート JSON に一切含まれない（sensitive マスクとは異なり、「存在しない」扱いになる点に注意）。
  - このエクスポートにも 6.4（アセット非包含保証）・6.5（sensitive マスク）のルールは引き続き適用される。

**【テンプレ説明】** 性能・容量・互換・権利・エラー処理など。

- **容量:** 1チェックポイント KB オーダー。数百件保持してもディスク影響が無視できること。
- **互換:** NDMF / MA / AAO の主要バージョンで動作。壊れやすいバージョン依存は明記。
- **権利・法務:**
  - 設定データにメッシュ/テクスチャ等の著作物を含めない設計を徹底。
  - 公開リポジトリへの誘導をしない。ドキュメント上も「ローカル完結が基本」と位置づける。
  - `.gitignore` テンプレートで購入アセットのコミット事故を防ぐ。
- **エラー処理:** 参照解決失敗・型変換失敗を握りつぶさず、ユーザーに可視化して選択させる。

---

## 8. 技術方針（概要）

**【テンプレ説明】** アーキテクチャの意思決定と理由。詳細設計は別ドキュメントに逃がす。

### 8.0 位置づけ: Unity Editor拡張としての内部構造

本プロダクトは広義には **Unity Editor 拡張**（VPM パッケージとして配布）だが、内部は役割の異なる2つのレイヤーで構成される。設計・実装時にこの分離を崩さないことが重要。

| レイヤー | 実装方式 | 役割 |
|---|---|---|
| ビルド時処理 | **NDMF プラグイン**（`Plugin<T>` 継承） | JSON 定義通りに Hierarchy を組み立てる／apply 処理の実行本体。Modular Avatar と同じビルドパイプラインに乗る |
| GUI / ユーザー操作 | **通常の Editor 拡張**（`EditorWindow`、`SerializedObject`） | 差分プレビュー、ローカル履歴パネル、Secret フィールド設定などの UI |

配布は VPM パッケージとして行い、VCC / ALCOM 経由でインストールされる（セクション10）。

- **NDMF プラグインとして実装。** MA 導入環境なら NDMF が入っているため追加障壁が低い。
- **Git ではなく独自の軽量スナップショットを採用する理由。** 管理対象はコードではなくパラメータ値であり情報量が少ない（セクション2参照）。汎用バージョン管理システムの複雑さ（コンフリクト解決、リポジトリ運用）を持ち込まずとも、参照パス＋値さえ保持すれば C# 側で読み書き・復元が完結する。
- **保存形式は JSON。** VPM エコシステムが JSON 文化であること、機械的な自動生成/適用に厳密さが向くこと、Newtonsoft.Json で辞書/ネストを自然に扱えることが理由。手編集の快適性を上げたい場合は JSONC/JSON5 を検討。
- **参照の安定化:** Hierarchy 内参照は Transform 相対パス、外部アセット参照は GUID + localID。InstanceID/fileID はセッションをまたぐと変わるため保存に使わない。
- **値の取得/適用:** SerializedObject / SerializedProperty 経由のリフレクションで汎用化し、独自コンポーネントにも対応。
  - **対応バージョン範囲:** 対応する MA / AAO のバージョン範囲を明記し、範囲外は警告表示（サイレント失敗させない）。
  - **未知のフィールドの扱い:** JSON に含まれるが現在のコンポーネントに存在しないフィールドは無視ではなく **警告を出す**（バージョン差異やタイポの早期発見のため）。
  - **参照解決のフォールバック（将来検討）:** Transform 相対パスは構造が異なるアバター/衣装では解決できないケースが頻発しうる。v1.x で「ファジーマッチによる近似解決」や「UI 上での手動再マッピング」を検討（セクション13）。
  - **Prefab オーバーライドとの整合性:** apply 時の書き込みが Prefab のオーバーライドとして正しくマークされるか（Prefab 元を意図せず書き換えないか）は、PoC 段階で必ず検証する（セクション12）。
- **差分:** JSON オブジェクトツリー同士の構造比較（DeepEquals 系）で apply 前プレビューを生成。
- **将来最適化:** フルスナップショット → 差分（イベントソーシング）方式へ拡張余地を残す。初期はフルスナップショットで十分軽い。

---

## 9. 競合 / 市場調査

**【テンプレ説明】** 直接競合・間接競合・代替手段を挙げ、自プロダクトの空白地帯を示す。

### 9.1 パッケージ/環境管理層（埋まっている）
- **VCC / ALCOM (vrc-get):** VPM パッケージ管理の完全代替。ALCOM はオープンソースで高速、複数プロジェクト一括更新に対応。→ **本ツールとは領域が異なり競合しない。むしろ土台。**

### 9.2 改変作業層（競合・参考）
- **Modular Avatar:** 非破壊着せ替えのデファクト。Setup Outfit で対応衣装はワンクリック。→ **本ツールは置換ではなく上に乗る。**
- **Avatar Optimizer (AAO):** 軽量化の定番。単発設定は強力。→ **設定の横展開層が空いている。**
- **キセテネ / キセテネ for MA:** スライダー着せ替え、非対応衣装向け。→ 着せ替え単機能。構成の記録/復元は守備範囲外。
- **連続アップロード系（ContinuousAvatarUploader / MyAvatarBuildSetting / VrcAvatarAutoUploader）:** 1セッション内で複数アバターを連投。→ 将来連携候補。
- **各種プリセット着替えシステム（早着替え等）:** VRChat 実機上の着替えメニュー生成が主眼で、Unity 上の構成管理とは別レイヤー。

### 9.3 コード駆動アプローチの既存例（思想的先行者）

本プロダクトと同じく「改変をコードで扱う」方向の取り組みは既に存在する。競合というより**補完関係**にあり、共存を前提に設計する。

| 手法 | 内容 | 本ツールとの関係 |
|---|---|---|
| **Git + `.gitignore`** | プロジェクト全体の差分管理。改変ごとにブランチを切る運用。技術寄り層にかなり普及 | **併用可能**（セクション2.3）。Git はファイル、本ツールは構成という意味単位を扱う |
| **Animator As Code 系** | FX レイヤーや表情を C# の fluent API で記述して自動生成。VRC で人気 | **レイヤーが異なる**（あちらはロジック生成、本ツールは構成の記録・復元）。生成された AnimatorController は「生成物」であり、本ツールの生成物 GC と共存させる必要がある（設計書 1.4.3） |
| **VCC カスタムテンプレート** | `package.json` + フォルダ構造で初期プロジェクトを定義 | **初回体験の補完候補**。本ツールは2回目以降に価値が出るため、テンプレートと組み合わせると初回から構成が整う |
| **Editor スクリプト + AssetDatabase** | C# でフォルダ階層・空 Prefab・Scene を一括作成。個人ツールとして存在 | 標準化されておらず、汎用ツールとしての空白は残る |
| **コンテナ / Nix による環境再現** | Unity + VCC 環境を宣言的に構築（主に Linux 勢） | **層が違う**（環境構築 vs アバター構成）。競合しない |
| **CI バッチビルド** | バッチモードでビルド＆アップロードを自動化 | VRChat の認証仕様上グレー。本ツールは非対応（セクション5.2） |

### 9.4 特定された空白地帯
> 「改変をコード化する」試みは Animator As Code（ロジック生成）や Git 運用（ファイル管理）として既に存在する。しかし **アバターの構成そのものを意味単位で宣言し、コンテナごとに記録・切り替え・横展開できるレイヤーは存在しない**。Git は `.prefab` の YAML 差分しか見せられず「髪が Long から Short に変わった」とは読めない。この**抽象度の差**が本プロダクトのコアバリューである。

---

## 10. 価格 / 配布モデル

**【テンプレ説明】** 有料/無料、配布経路、ライセンス制御を書く。収益目標がある場合はここに明記する。

- **方針: 収益最大化ではなく「お布施」型。** 本業/儲け目的ではなく、開発継続のモチベーション維持が主目的。
- **基本無料 + 任意の投げ銭（100〜300円程度）** を BOOTH で用意する形を想定。
  - 無料版でコア機能（スナップショット保存・apply・ローカル履歴・安全エクスポート）はフル提供。
  - 投げ銭は「機能アンロックの対価」ではなく、応援・カンパの位置づけ（過度な機能制限で事実上の有料化にしない）。
- ライセンス制御を厳密にする必要性は低いため、**購入者限定トークン配布などの複雑な仕組みは不要**。VPM リポジトリは通常どおり公開でよい（無料配布ツールと同じ導線）。
- BOOTH は「投げ銭を受け取る窓口」として使い、VPM 経由のインストール導線は無料ツールと同様にシンプルに保つ。
- 中級者ターゲットゆえ、GUI を過剰に作り込まず JSON 直編集を許容することで開発コストを抑える（収益目的でないため、工数対効果はより重要）。
- 将来的に機能が育って需要が増えた場合、有料版・投げ銭型のどちらを継続するかは v1.1 以降で再検討（セクション13参照）。

---

## 11. リスクと対応

**【テンプレ説明】** 技術・法務・市場リスクを、影響度と緩和策つきで。

| リスク | 影響 | 緩和策 |
|---|---|---|
| **既存の Git 運用で満足している層に刺さらない** | 需要不足 | 「Git より簡単」ではなく「Git では届かない粒度」を訴求（セクション2.3）。併用可能であることを明示 |
| **Animator As Code 等の生成物と競合し、生成アセットが二重管理になる** | 動作不良 | 生成物 GC の対象を本ツール生成分に限定。他ツール生成物は識別して触らない |
| 工数に対して収益が見合わずモチベ低下 | 開発停滞 | 収益目的でないと割り切り、機能追加は無理のない範囲に絞る（セクション10） |
| 設定データが配布物規約に抵触する解釈 | 法務/炎上 | メッシュ非包含を徹底、公開共有を推奨しない、規約確認の注意表示 |
| .gitignore 漏れでアセット本体が流出 | 明確な規約違反 | テンプレート同梱、ローカル完結を既定に |
| MA/AAO のバージョン変更で破綻 | 動作不良 | 対応バージョン明記、SerializedObject 経由で疎結合化 |
| 参照解決失敗による復元事故 | データ破損感 | apply/復元前の差分プレビューと部分適用 |
| ボーン自動フィットの精度不足 | 期待外れ | 自動＋手動微調整の併用、MVP から外して段階導入 |

---

## 12. マイルストーン（暫定）

**【テンプレ説明】** MVP → 拡張の順で。日付ではなくフェーズで切ると変更に強い。

1. **PoC:** SerializedObject 経由で1コンポーネントの取得/適用が往復できることを検証（技術的コア）。あわせて **Prefab オーバーライドとして正しく書き込まれるか**（Prefab 元を意図せず書き換えないか）を確認する。
2. **MVP（実装優先順位）:**
   1. SerializedObject 取得/適用往復 ＋ 基本 JSON スキーマ（`schemaVersion` 付き）
   2. 差分プレビューの構造化表示（Terraform plan らしさの生命線、セクション6.2）
   3. 安全なエクスポート（アセット非包含の保証、内部/外部シリアライズ分離）
   4. ローカル履歴
   5. Secret フラグ（エクスポート時のみ、ユーザー明示フラグのみ）
3. **v1.1:** 軽量化プリセットの横展開、複数アバター一括適用。
4. **v1.2:** プリセットキュー＋連続ビルド連携（半自動）。
5. **v1.x:** プリセット（テンプレ）の共有・販売機能（セクション5.3）。参照解決のファジーマッチ/手動再マッピングもこの時期に検討。

---

## 13. 未解決の論点 / 要検討

**【テンプレ説明】** 決めきれていないことを正直に残す。レビューの起点になる。

- JSON 標準 or JSONC/JSON5 のどちらを採るか（手編集体験と実装コストのバランス）。
- 差分方式（イベントソーシング）をどの段階で導入するか。
- プリセット共有を将来提供する場合、規約的に安全な共有単位をどう定義するか。
- Secret フィールドの自動検出をいつ導入するか（MVP はユーザー明示フラグのみ、セクション6.5）。
- Unity 標準 Undo（`Undo.RecordObject`）との統合を将来的に行うか、ツール独自履歴のみで押し通すか。
- 参照解決のファジーマッチ / 手動再マッピング UI の実装方式（v1.x、セクション8）。

---

## 14. 拡張の可能性: オープンフォーマットとしての規格化

**【テンプレ説明】** MVPの範囲外だが、プロダクトの中長期的なポジショニングに関わる構想はここに隔離しておく。ロードマップには含めないが、設計判断（特にJSONスキーマの互換性維持）に影響するため記録しておく。

本ツールが管理する JSON はアセット本体を含まない軽量なデータであるため、単なる「ツール内部の保存形式」を超えて、**VRChat 改変設定の交換フォーマット**として機能しうる。VPM が `package.json` ベースの規格としてエコシステムに定着したのと同様のパターン。

- 将来的に他の開発者が同じ JSON スキーマを読み書きできれば、BOOTH のテンプレ販売がこの規格に統一され、購入者は「対応ツールで読み込むだけ」で使えるようになる可能性がある。
- セクション5.3（プリセット共有）・セクション6.5（Secret フィールド）はいずれもこの規格の一部として設計する。
- **設計上の含意:** MVP 時点からスキーマのバージョニング（例: `"schemaVersion": 1`）を JSON に含めておき、将来の破壊的変更に備える。JSON Schema（`.schema.json`）としてスキーマ定義を早期に文書化しておくと、後からの規格化・他ツールとの連携がしやすくなる。加えて、VSCode 等での JSON 直編集時に入力補完・バリデーションが効くようになるため、ペルソナC（技術寄り個人勢）の体験向上という即効性のある副次効果もあり、コストの割にリターンが大きい施策。
- ただしこれはあくまで「そうなったら良い」という中長期の可能性であり、MVP のスコープには含めない。過度に汎用化を意識して実装を複雑にしないこと（YAGNI を優先し、スキーマバージョニングの器だけ用意する）。
