# βテストのお願い — avatar-vcs 0.8.0-poc

テストに協力してくださってありがとうございます。このページはβテスターの方向けの案内です。

avatar-vcs は VRChat アバターの「構成」——衣装や髪型の Prefab 構成、BlendShape、
マテリアル設定——を Git のようにコミット・ブランチで管理する Unity エディタ拡張です。
まだ PoC 段階で、**実際のアバターで使われた回数がまだ少ない**のが今の一番のリスクです。
そこを埋めるのがこのβテストの目的です。

---

## 0. 始める前に — バックアップを取ってください ★必須

**本番のアバタープロジェクトでいきなり試さないでください。**

このツールはシーン上のアバターを実際に書き換えます（オブジェクトの破棄・再生成、
マテリアルスロットの差し替え）。過去に、コミットを削除するとアバターが着ている
マテリアルまで消えるバグを出しています（0.7.0-poc で修正済み）。同種のバグが
まだ残っている可能性は十分あります。

推奨する順番:

1. Unity プロジェクトのフォルダごとコピーして、**コピーのほうで試す**
2. コピーで一通り動くと確信できてから、本番プロジェクトへ

最低限バックアップすべきもの:

| 対象 | 理由 |
| --- | --- |
| `Assets/` | マテリアル・Prefab の実体。ツールが書き換える対象 |
| `ProjectSettings/` | コミット履歴が `ProjectSettings/AvatarVcs/` に入る |

> プロジェクトを Git で管理している場合は、試す直前にコミットしておけば十分です。

---

## 1. インストール

VCC (VRChat Creator Companion) の **Settings → Packages → Add Repository** に
以下の URL を追加してください:

```text
https://raw.githubusercontent.com/whitegauss/avatar-vcs/main/index.json
```

追加後、プロジェクトの **Manage Packages** から **"Avatar VCS (Phase 1 PoC)"** を
追加します。バージョンが **0.8.0-poc** になっていることを確認してください。

- 動作環境: Unity **2022.3** 系（VRChat 推奨環境）
- VCC を使わない場合の入れ方は [docs/getting-started.md](./docs/getting-started.md) にあります

---

## 2. まず触ってほしいこと（10分ほど）

細かい機能を網羅する必要はありません。**この一周が普通に通るか**が知りたいことです。

1. Hierarchy でアバターのルートを選択 → `GameObject → AvatarVCS → Ensure Root`
2. `Window → AvatarVCS` でウィンドウを開き、Avatar 欄にアバターのルートをアサイン
3. 下の Commit バーにメッセージを書いて **Commit**（これが初期状態）
4. アバターを何か変える。分かりやすいのは:
   - 衣装 Prefab を `[AvatarVCS]` の下に入れる
   - lilToon マテリアルの色を変える
   - BlendShape のスライダーを動かす
5. もう一度 **Commit**
6. 履歴から **3 のコミットを選んで Checkout** → **元に戻ること**
7. もう一度 5 のコミットを Checkout → **変更後の状態に戻ること**

6 と 7 の往復が、このツールの成立条件です。ここが崩れたら、それが一番重要な報告になります。

---

## 3. 0.8.0-poc で特に見てほしいところ

### 3-1. マテリアル設定プリセット（今回の新機能）

マテリアルのシェーダー設定だけを小さなファイルに書き出して、別のマテリアルに読み込めます。

1. Project ウィンドウでマテリアルを右クリック → **AvatarVCS → Export Material Settings...**
2. 適当な場所に JSON として保存
3. **別の**マテリアル（できれば別アバターのもの）を右クリック → **AvatarVCS → Import Material Settings...**
4. 2 のファイルを選ぶ → 見た目が近づくこと、Console に適用件数とスキップ内容が出ること

> インポートは**選んだマテリアルそのものを書き換えます**（複製は作りません）。
> 意図した動作ですが、間違えたら Ctrl+Z で戻せることも確認してみてください。

見てほしいのは:

- 書き出したファイルが**読める大きさ**か（lilToon は約490プロパティありますが、
  デフォルトと違うものだけ記録するので数十行程度になるはずです）
- **シェーダーが違うマテリアルに読み込んだとき**、無いプロパティが握りつぶされずに
  Console へ報告されるか
- **持っていないテクスチャを参照するプリセット**を読み込んだとき、その項目だけ
  スキップされて他は適用されるか

> テクスチャは GUID だけを記録し、画像そのものは入りません。つまりプリセットを
> 誰かに渡しても、相手が持っていないテクスチャが復元されることはありません。
> 有料アセットを含むマテリアルでも、`.mat` を直接渡すより安全という設計です。
> **ここが本当にそうなっているか**は、ぜひ疑って確かめてください。

### 3-2. コミットメモ

コミットを選んで、ウィンドウ下部の **Note** を開くと、後から書き換えられる自由記述欄があります。
「これは衣装A＋髪B、肩のトグルはオフ」のような覚え書き用です。
0.8.0-poc から**既定で閉じた状態**になりました。

- 書いて Save → 別コミットへ移動 → 戻ってきて残っているか
- 書きかけのまま別コミットを選んだとき、書きかけが**他のコミットに移らない**か

### 3-3. ウィンドウの重さ

**Changes パネルが閉じている状態**で、Inspector のスライダーを連続で動かしてみてください。
ウィンドウを開いたままでも重くならないはずです。ここは以前の版で明確に重かった箇所なので、
「まだ重い」という報告はそれ自体が有効な情報です。

---

## 4. 既知の制限（バグではありません）

- **アセットの実体は保存しません。** 「昔のバージョンの Prefab に戻す」ことはできません。
  記録しているのは「どの構成がどのアセットを参照していたか」だけです。
  Prefab そのものを編集して壊した場合、このツールでは戻せません。
- **ボーンの pose（Transform）は対象外です。** 動かしてもコミットに含まれません。
- **マテリアル設定は lilToon 系を中心に対応しています。** 対応外のシェーダーは
  「参照しているマテリアルはどれか」までは記録しますが、シェーダー設定の値までは復元しません。
- **マテリアル設定プリセットは、シェーダーのデフォルトと違う値だけを記録します。**
  相手のマテリアルで既にデフォルトどおりの項目は、書かれていない＝そのままで正しい、という意味です。
- **プリセットにテクスチャの Tiling / Offset は含まれません。** 記録するのは
  どのテクスチャを指しているか（GUID）までです。
- **Unity 2022.3 系のみ**で確認しています。

---

## 5. 不具合の報告

見つけたら、**その場でプロジェクトを触るのをやめて**、以下を送ってください。
報告先は個別にお伝えしている DM です。

### 送ってほしいもの

1. **何をしたら何が起きたか。** できれば手順を1から。
   「Checkout したら衣装が消えた」だけでも、無いよりずっと役に立ちます
2. **Console の全文。** 赤いエラーだけでなく、その前後の黄色い警告とスタックトレースも。
   Console を右クリック → コピーで全部取れます
3. **バージョン。** Unity のバージョンと avatar-vcs のバージョン（0.8.0-poc）
4. **使っているシェーダー。** lilToon か、そのバリアントか、別のものか
5. **可能なら履歴ファイル。** `ProjectSettings/AvatarVcs/avatars/<英数字の長い名前>/` フォルダを
   zip にしたもの

### 5 が特に効きます

`ProjectSettings/AvatarVcs/avatars/<guid>/commits/<id>.json` には、そのコミットが
実際に何を記録したかがそのまま入っています。これがあると
**「そもそも記録されていなかった」のか「記録はされたが復元されなかった」のか**が
一意に分かります。この2つは原因がまったく別で、これが分からないと調査が止まります。

このファイルにはアバターやアセットの名前が含まれます。見られたくない場合は
無理に送らなくて大丈夫です。1〜4 だけでも十分に助かります。

### 「これは報告していいのか」で迷ったら

してください。以下は全部、有効な報告です。

- 動くけれど分かりにくい、どこを押せばいいか迷った
- ドキュメントを読んでも意味が取れなかった
- 期待と違う動きをしたが、バグなのか仕様なのか判断できない
- 重い、待たされる
- そもそも使う気にならなかった（**これが一番聞きたいことかもしれません**）

---

## 6. 収集しているデータについて

**何も収集していません。** 使用状況の送信、エラーの自動送信、その他の通信を
一切行いません。このツールが書き込むのは、あなたのプロジェクトの中の
`ProjectSettings/AvatarVcs/` だけです。

アバターやアセットの名前は、どこから何を買ったかを表してしまいます。
自動で送るべきものではないと考えているので、送っていません。
その代わり、不具合の情報は上記のように**手で送っていただく**形になります。手間をかけます。

---

## 参考ドキュメント

- [はじめに (Getting Started)](./docs/getting-started.md) — インストールと初回セットアップ
- [ユーザーガイド (User Guide)](./docs/user-guide.md) — 各パネル・機能のリファレンス
- [よくある質問 (FAQ)](./docs/faq.md) — トラブルシューティング
- [変更履歴 (CHANGELOG)](./CHANGELOG.md) — 各バージョンで何が変わったか
